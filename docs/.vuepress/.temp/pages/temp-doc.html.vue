<template><p>Network Compu
ting Architecture，NCA</p>
<p>（，I
PC。广义上讲，可以认为 RPC 属于 IPC 的一种特例，
JAR、WAR、DLL
JNI
SOAP 协议 ，SOAP 1.0（Simple Object Access Protocol）规范的发</p>
<p>EJB
RMI
DNS
是   REST，即“”的 ）</p>
<p>SOAP、WSDL、UDDI 协议
XML
HTTP 协议
之上，有的能直接运行于传输层 TCP/UDP 协议之
JSON 的
Hessian
URI</p>
<p>的坑。
《RESTful Web APIs 》和《RESTful Web Services 》的作者 Leonard Richardson 曾提
出过一个衡量“服务有多么 REST”的 Richardson 成熟度模型（Richardson Maturity Model
GET /users/icyfenix/cart/2
https://icyfenix.cn REST 设计风格
），便于那些原本不使用 REST 的系统，能够逐步地导入 REST。Richardson 将服务接
口“REST 的程度”从低到高，分为 0 至 3 级</p>
<p>ACID原子性（Atomic）：在同一项业务处理过程中，事务保证了对多个数据的修改，要么同
时成功，要么同时被撤销。
隔离性（Isolation）：在不同的业务处理过程中，事务保证了各自业务正在读、写的数
据互相独立，不会彼此影响。
持久性（Durability）：事务应当保证所有成功被提交的数据修改都能够正确地被持久
化，不丢失数据</p>
<p>JDBC</p>
<p>是 DTP（Distributed Transaction Processing ）模型中的概念</p>
<p>、JMS 等等）如果想要支持 JTA，只</p>
<p>了JSR-*</p>
<p>被称为“两段式提交 ”（2 Phase Commit，2PC）协议，而
出了“三段式提交 ”（3 Phase Commit，3PC）协议</p>
<p>JNDI</p>
<p>JDBC、ORM、JMS 等
BASE 分别是基本可用性（Basically Availabl
e）、柔性事务（Soft State）和最终一致性（Eventually Consistent）的缩写。</p>
<p>ACK
TCC 事务Try-Confirm-Cancel</p>
<p>并成为万维网联盟 （World Wide Web Consortium，W3C）</p>
</template>
